#include <stdio.h>

/* Este programa es solo un ejemplo de como usar variables y printf; así como líneas nuevas. */
int main( void )
{
    int num;
    num = 1;
    printf( "Soy una simple " );
    printf( "computadora\n" );
    
    printf( "Mi número favorito es %d porque es el primero.\n", num );

    return 0;
}
